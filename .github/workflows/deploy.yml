name: Liste des fichiers sur Ubuntu

on:
  push:
    branches:
      - main

jobs:
  list-files:
    runs-on: ubuntu-latest

    steps:
      - name: Afficher le contenu de la racine
        run: |
          ls -la /

      - name: Afficher le contenu du répertoire courant
        run: |
          ls -la
