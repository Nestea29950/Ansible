name: Vérifier la machine virtuelle

on:
  push:
    branches:
      - main

jobs:
  check-env:
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Cloner le repository
      - name: Cloner le repository
        uses: actions/checkout@v3

      # Étape 2 : Afficher le contenu du répertoire courant
      - name: Lister les fichiers
        run: |
          ls -la
